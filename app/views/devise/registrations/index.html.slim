.container
  .row
    h3
      | Users
      
      = search_form_for @q do |f|
        

      table#users
        thead
          tr
            th = sort_link @q, :last_name, "Last Name", default_order: :desc
            th = sort_link @q, :first_name, "First Name"
            th = sort_link @q, :email, "Email"
            th = sort_link @q, :role, "Role"
            th = "Operations"
        tbody
           - for usr in @users do
            tr
              td = link_to "#{usr.last_name}",  userx_registration_path(usr)
              td = usr.first_name
              td = usr.email
              td = usr.role
              /TODO Add icons for operations
              td = link_to('Edit', edit_userx_registration_path(usr), id: "edit_#{usr.id}") + " | " + link_to('Delete', user_registration_path(usr), id: "delete_#{usr.id}", :data => { :confirm => "Are you sure?" }, method: :delete)
              /TODO Create test for delete
        = paginate(@users)
        
